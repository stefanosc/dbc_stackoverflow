.row
  .col-xs-3.col-sm-2.col-md-2.answer-vote-container
    .vote.well(id= "answer#{answer.id}")= answer.votes
    = link_to vote_question_answer_path(@question, answer, vote: 1, format: :json),method: :patch, remote: true, class: "answer-vote up" do
      %i.glyphicon.glyphicon-thumbs-up
    = link_to vote_question_answer_path(@question, answer, vote: -1, format: :json),method: :patch, remote: true, class: "answer-vote down" do
      %i.glyphicon.glyphicon-thumbs-down
  .col-sm-10.col-md-8.col-xs-9.col-lg-7
    %h2= answer.title
    %p= answer.content
.row
  .col-xs-offset-3.col-sm-offset-2
    = link_to "delete", ([@question, answer]), method: :delete, class: "btn btn-default delete-answer"

