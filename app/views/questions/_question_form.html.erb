<%= form_for @question do |f| %>
  <%= f.text_field :title %>
  <%= f.text_area :content, size: "60x12" %>
  <% button_text = @question.new_record? ? "Create" : "Edit" %>
  <%= f.submit "#{button_text} Question" %>
<% end %>
